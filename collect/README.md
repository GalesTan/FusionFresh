# 蓝牙多传感器 + 摄像头采集

原 `fusionfresh_main`。配置、协议、固件未改。

```powershell
# 仓库根目录
python collect/main.py

# 或进入本目录
python main.py
```

详见 [docs/hardware.md](../docs/hardware.md)。`data/` 初始为空，运行后才会写入 latest 与 CSV。
